{ // Short Circuit
	"Short Circuit",
	[](const Widget::DrawArgs &args, Vec size) {
		float upper = size.y / 8;
		float lower = size.y / 2;
		float center = size.x / 2;
		nvgStrokeColor(args.vg, SUBLIGHTBLUE);
		nvgBeginPath(args.vg);	
		nvgMoveTo(args.vg, -5, upper);
		nvgLineTo(args.vg, center, upper);
		nvgLineTo(args.vg, center, lower);
		nvgLineTo(args.vg, size.x - 5, lower);
		nvgStrokeWidth(args.vg, 2);
		nvgStroke(args.vg);	
		drawConnector(args.vg, size.x - 5, lower, nvgRGB(0xff, 0xff, 0xff));
	},
	[] (status_t a,
		status_t b,
		status_t c,
		status_t d,
		status_t a0,
		status_t b0,
		status_t c0,
		status_t d0) -> status_t {
		return a;
	}
}
